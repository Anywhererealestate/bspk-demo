import { Card } from '@bspk/ui/Card';
import { Block, BlockExample } from 'src/utils/blocks';

export const example = (
    <Block name="Cards">
        <BlockExample name="Default">
            <Card>
                This is a simple card example. Cards are used to group related information together in a visually
                distinct container.
            </Card>
        </BlockExample>
    </Block>
);
